backend/static/netrange.apk
backend/data/
__pycache__/
*.pyc
venv/
node_modules/
.expo/
.git/
*.log
.DS_Store
# Built APKs and local helper scripts. .easignore replaces .gitignore for EAS
# uploads, so anything only excluded in .gitignore would otherwise be shipped
# to the build servers.
builds/
bun-install.sh
