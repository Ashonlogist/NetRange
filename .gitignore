__pycache__/
*.pyc
venv/
data/scans.json
*.egg-info/
dist/
build/

node_modules/
.expo/
*.log
.DS_Store
*.pem
.env
.vscode/
.idea/
# Anchored to the repo root on purpose. A bare `android/` also matches
# modules/*/android/, which silently hid every Kotlin file of the local
# telephony module -- the config committed, the module it points at did not.
/android/
backend/static/netrange.apk
